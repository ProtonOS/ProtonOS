# ProtonOS Driver Configuration
# Drivers to load from /drivers after root mount
# Format: one driver filename per line

# Additional drivers loaded after root mount
# (none yet - add driver DLL names here)
